{
  "name": "frrr-workspace",
  "private": true,
  "scripts": {
    "start:server": "cross-env PORT=4000 PUBLIC_BASE_URL=http://localhost:3001 npm --prefix server start",
    "start:client": "cross-env PORT=3001 REACT_APP_API_URL=http://localhost:4000 npm --prefix client start",
    "start-all": "concurrently -n server,client -c cyan,green \"npm run start:server\" \"npm run start:client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0"
  }
}
